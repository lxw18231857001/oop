1.特点：重用性  灵活性 扩展性
2.在类里面$this 可以理解为这个类的一个实例
3.当实例化类时，这个类的构造=方法被调用__construct()
4.对象被销毁时，调用的方法是，__destruct()
5.类常量是属于这个类的，不是属于对象的
6.会受到命名空间影响的：类（抽象类，traits）,接口，函数，常量
7.__autoload 自动加载类文件
8.后期绑定static,根据上下文判断指向那个类
9.对象复制：
     浅拷贝：变量之间地址传递,省内存
     深拷贝：把变量复制了一份，传递给另外一个值，变量之间传递值 clone
10.trait 代码重用